[build-system]
requires = ["setuptools>=40.8.0", "wheel", "cmake", "pybind11"]
build-backend = "setuptools.build_meta"

[tool.coverage.run]
branch = true
include = ["src/*"]
[tool.coverage.paths]
source = ["src"]